export default class User {
    constructor(username, email, password, age, genres, imageURL) {
        this.username = username;
        this.email = email;
        this.password = password;
        this.age = age;
        this.genres = genres;
        this.imageURL = imageURL;
    }        
}